-- ============================================
-- FPS Mobile Cheat - Enhanced Aimbot v3
-- Fixed: FOV Circle Exact Screen Center
-- Enhanced: Smooth Aim, Bone Select, Target Lock
-- ============================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local StarterGui = game:GetService("StarterGui")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- Notification
local function Notify(title, text, duration)
    StarterGui:SetCore("SendNotification", {
        Title = title,
        Text = text,
        Duration = duration
    })
end

-- Settings
local genv = getgenv()
genv.FPSCheat = genv.FPSCheat or {
    ESP = false,
    Aimbot = false,
    ThirdPerson = false,
    FOV = 70,
    AimbotRange = 200,
    AimSpeed = 0.15,
    AimbotFOV = 70,
    ESPColor = Color3.fromRGB(255, 0, 0),
    TeamCheck = true
}
local Settings = genv.FPSCheat

-- UI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FPSMobileCheatV3"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
pcall(function() ScreenGui.Parent = CoreGui end)
if not ScreenGui.Parent then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 240, 0, 370)
MainFrame.Position = UDim2.new(0.5, -120, 0.5, -185)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

-- Title Bar
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 38)
TitleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame
Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 10)

local TitleText = Instance.new("TextLabel")
TitleText.Size = UDim2.new(1, -80, 1, 0)
TitleText.Position = UDim2.new(0, 12, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "FPS Cheat"
TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleText.TextSize = 16
TitleText.Font = Enum.Font.GothamBold
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.Parent = TitleBar

-- Minimize
local MinBtn = Instance.new("TextButton")
MinBtn.Size = UDim2.new(0, 32, 0, 32)
MinBtn.Position = UDim2.new(1, -70, 0, 3)
MinBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
MinBtn.Text = "-"
MinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinBtn.TextSize = 18
MinBtn.Font = Enum.Font.GothamBold
MinBtn.Parent = TitleBar
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 6)

-- Close
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 32, 0, 32)
CloseBtn.Position = UDim2.new(1, -36, 0, 3)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = TitleBar
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 6)

-- Content
local Content = Instance.new("Frame")
Content.Size = UDim2.new(1, -20, 1, -48)
Content.Position = UDim2.new(0, 10, 0, 43)
Content.BackgroundTransparency = 1
Content.Parent = MainFrame

-- Toggle Factory
local function CreateToggle(name, key, y)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1, 0, 0, 38)
    f.Position = UDim2.new(0, 0, 0, y)
    f.BackgroundTransparency = 1
    f.Parent = Content

    local l = Instance.new("TextLabel")
    l.Size = UDim2.new(0.6, 0, 1, 0)
    l.BackgroundTransparency = 1
    l.Text = name
    l.TextColor3 = Color3.fromRGB(210, 210, 210)
    l.TextSize = 14
    l.Font = Enum.Font.Gotham
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.Parent = f

    local b = Instance.new("TextButton")
    b.Size = UDim2.new(0, 52, 0, 28)
    b.Position = UDim2.new(1, -52, 0.5, -14)
    b.BackgroundColor3 = Settings[key] and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(70, 70, 70)
    b.Text = Settings[key] and "ON" or "OFF"
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    b.TextSize = 12
    b.Font = Enum.Font.GothamBold
    b.Parent = f
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)

    b.MouseButton1Click:Connect(function()
        Settings[key] = not Settings[key]
        b.BackgroundColor3 = Settings[key] and Color3.fromRGB(0, 180, 0) or Color3.fromRGB(70, 70, 70)
        b.Text = Settings[key] and "ON" or "OFF"
        Notify("FPS Cheat", name .. " " .. (Settings[key] and "Enabled" or "Disabled"), 2)
    end)
end

CreateToggle("ESP / Wallhack", "ESP", 0)
CreateToggle("Aimbot", "Aimbot", 42)
CreateToggle("Third Person", "ThirdPerson", 84)

-- Slider Factory
local activeSlider = nil

local function CreateSlider(name, key, y, min, max, suffix, isFloat)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1, 0, 0, 55)
    f.Position = UDim2.new(0, 0, 0, y)
    f.BackgroundTransparency = 1
    f.Parent = Content

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, 0, 0, 22)
    lbl.BackgroundTransparency = 1
    lbl.Text = name .. ": " .. (isFloat and string.format("%.2f", Settings[key]) or Settings[key]) .. (suffix or "")
    lbl.TextColor3 = Color3.fromRGB(210, 210, 210)
    lbl.TextSize = 14
    lbl.Font = Enum.Font.Gotham
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = f

    local track = Instance.new("Frame")
    track.Size = UDim2.new(1, 0, 0, 10)
    track.Position = UDim2.new(0, 0, 0, 32)
    track.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    track.BorderSizePixel = 0
    track.Parent = f
    Instance.new("UICorner", track).CornerRadius = UDim.new(0, 5)

    local fill = Instance.new("Frame")
    local percent = (Settings[key] - min) / (max - min)
    fill.Size = UDim2.new(percent, 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
    fill.BorderSizePixel = 0
    fill.Parent = track
    Instance.new("UICorner", fill).CornerRadius = UDim.new(0, 5)

    local knob = Instance.new("TextButton")
    knob.Size = UDim2.new(0, 22, 0, 22)
    knob.Position = UDim2.new(percent, -11, 0, -6)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.Text = ""
    knob.Parent = track
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local function UpdateFromInput(input)
        local absX = track.AbsolutePosition.X
        local absW = track.AbsoluteSize.X
        local pos = math.clamp((input.Position.X - absX) / absW, 0, 1)
        local val = isFloat and (min + pos * (max - min)) or math.floor(min + pos * (max - min))
        Settings[key] = isFloat and math.floor(val * 100) / 100 or val
        lbl.Text = name .. ": " .. (isFloat and string.format("%.2f", Settings[key]) or Settings[key]) .. (suffix or "")
        fill.Size = UDim2.new(pos, 0, 1, 0)
        knob.Position = UDim2.new(pos, -11, 0, -6)
        if key == "FOV" then Camera.FieldOfView = Settings.FOV end
    end

    knob.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseButton1 then
            activeSlider = {track = track, fill = fill, knob = knob, lbl = lbl, min = min, max = max, key = key, suffix = suffix, isFloat = isFloat, name = name}
        end
    end)
    track.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseButton1 then
            activeSlider = {track = track, fill = fill, knob = knob, lbl = lbl, min = min, max = max, key = key, suffix = suffix, isFloat = isFloat, name = name}
            UpdateFromInput(i)
        end
    end)

    return f
end

CreateSlider("FOV", "FOV", 128, 30, 120, "", false)
CreateSlider("Aim Range", "AimbotRange", 188, 50, 500, "", false)
CreateSlider("Aim Speed", "AimSpeed", 248, 0.01, 0.5, "", true)
CreateSlider("Aimbot FOV", "AimbotFOV", 308, 20, 200, "px", false)

-- Slider Input Logic
UserInputService.InputChanged:Connect(function(i)
    if activeSlider and (i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseMovement) then
        local s = activeSlider
        local absX = s.track.AbsolutePosition.X
        local absW = s.track.AbsoluteSize.X
        local pos = math.clamp((i.Position.X - absX) / absW, 0, 1)
        local val = s.isFloat and (s.min + pos * (s.max - s.min)) or math.floor(s.min + pos * (s.max - s.min))
        Settings[s.key] = s.isFloat and math.floor(val * 100) / 100 or val
        s.lbl.Text = s.name .. ": " .. (s.isFloat and string.format("%.2f", Settings[s.key]) or Settings[s.key]) .. (s.suffix or "")
        s.fill.Size = UDim2.new(pos, 0, 1, 0)
        s.knob.Position = UDim2.new(pos, -11, 0, -6)
        if s.key == "FOV" then Camera.FieldOfView = Settings.FOV end
    end
end)

UserInputService.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseButton1 then
        activeSlider = nil
    end
end)

-- Drag
local dragging = false
local dragStart, startPos

TitleBar.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = i.Position
        startPos = MainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(i)
    if dragging and (i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseMovement) then
        local delta = i.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.Touch or i.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

-- Minimize
local minimized = false
MinBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    Content.Visible = not minimized
    MainFrame.Size = minimized and UDim2.new(0, 240, 0, 38) or UDim2.new(0, 240, 0, 370)
    MinBtn.Text = minimized and "+" or "-"
end)

-- Cleanup
CloseBtn.MouseButton1Click:Connect(function()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local h = p.Character:FindFirstChild("FPSESP_H")
            if h then h:Destroy() end
            local b = p.Character:FindFirstChild("FPSESP_B")
            if b then b:Destroy() end
        end
    end
    if AimbotCircle then AimbotCircle:Destroy() end
    ScreenGui:Destroy()
    Notify("FPS Cheat", "Script Closed", 3)
end)

-- ============================================
-- AIMBOT FOV CIRCLE (Exact Screen Center)
-- ============================================
local AimbotCircle = Instance.new("Frame")
AimbotCircle.Name = "AimbotFOVCircle"
AimbotCircle.BackgroundTransparency = 0.88
AimbotCircle.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
AimbotCircle.BorderSizePixel = 0
AimbotCircle.Visible = false
AimbotCircle.ZIndex = 0
AimbotCircle.Parent = ScreenGui

local cc = Instance.new("UICorner", AimbotCircle)
cc.CornerRadius = UDim.new(1, 0)

local CircleOutline = Instance.new("UIStroke", AimbotCircle)
CircleOutline.Thickness = 2.5
CircleOutline.Color = Color3.fromRGB(255, 80, 80)

-- Update circle every frame to stay at exact center
RunService.RenderStepped:Connect(function()
    if Settings.Aimbot then
        local size = Settings.AimbotFOV * 2
        local cx = Camera.ViewportSize.X / 2
        local cy = Camera.ViewportSize.Y / 2
        AimbotCircle.Size = UDim2.new(0, size, 0, size)
        AimbotCircle.Position = UDim2.new(0, cx - size/2, 0, cy - size/2)
        AimbotCircle.Visible = true
    else
        AimbotCircle.Visible = false
    end
end)

-- ============================================
-- ESP SYSTEM
-- ============================================
local ESPObjects = {}

local function ClearPlayerESP(player)
    if player and player.Character then
        local h = player.Character:FindFirstChild("FPSESP_H")
        if h then h:Destroy() end
        local b = player.Character:FindFirstChild("FPSESP_B")
        if b then b:Destroy() end
    end
    ESPObjects[player] = nil
end

local function CreateESP(player)
    if player == LocalPlayer then return end
    if not player.Character then return end
    if Settings.TeamCheck and player.Team == LocalPlayer.Team then
        ClearPlayerESP(player)
        return
    end

    local char = player.Character
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not humanoid or humanoid.Health <= 0 then
        ClearPlayerESP(player)
        return
    end

    if char:FindFirstChild("FPSESP_H") and char:FindFirstChild("FPSESP_B") then
        local bg = char:FindFirstChild("FPSESP_B")
        if bg then
            local distLabel = bg:FindFirstChild("DistLabel")
            local nameLabel = bg:FindFirstChild("NameLabel")
            if distLabel and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local dist = math.floor((LocalPlayer.Character.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude)
                distLabel.Text = dist .. "m"
            end
            if nameLabel then
                nameLabel.TextColor3 = Settings.ESPColor
            end
            local hl = char:FindFirstChild("FPSESP_H")
            if hl then
                hl.FillColor = Settings.ESPColor
            end
        end
        return
    end

    ClearPlayerESP(player)

    local hl = Instance.new("Highlight")
    hl.Name = "FPSESP_H"
    hl.FillColor = Settings.ESPColor
    hl.OutlineColor = Color3.fromRGB(255, 255, 255)
    hl.FillTransparency = 0.65
    hl.OutlineTransparency = 0
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Adornee = char
    hl.Parent = char

    local bg = Instance.new("BillboardGui")
    bg.Name = "FPSESP_B"
    bg.Size = UDim2.new(0, 120, 0, 50)
    bg.StudsOffset = Vector3.new(0, 3.5, 0)
    bg.AlwaysOnTop = true
    bg.Parent = char

    local nameLabel = Instance.new("TextLabel")
    nameLabel.Name = "NameLabel"
    nameLabel.Size = UDim2.new(1, 0, 0.5, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = player.Name
    nameLabel.TextColor3 = Settings.ESPColor
    nameLabel.TextSize = 13
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.Parent = bg

    local distLabel = Instance.new("TextLabel")
    distLabel.Name = "DistLabel"
    distLabel.Size = UDim2.new(1, 0, 0.5, 0)
    distLabel.Position = UDim2.new(0, 0, 0.5, 0)
    distLabel.BackgroundTransparency = 1
    distLabel.Text = "0m"
    distLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    distLabel.TextSize = 11
    distLabel.Font = Enum.Font.Gotham
    distLabel.Parent = bg

    ESPObjects[player] = {Highlight = hl, Billboard = bg, Character = char}
end

local function ClearAllESP()
    for _, p in pairs(Players:GetPlayers()) do
        ClearPlayerESP(p)
    end
    ESPObjects = {}
end

-- ============================================
-- ENHANCED AIMBOT SYSTEM
-- ============================================

-- Get best bone to aim at (priority order)
local function GetTargetBone(character)
    local priority = {"Head", "UpperTorso", "Torso", "HumanoidRootPart"}
    for _, boneName in ipairs(priority) do
        local bone = character:FindFirstChild(boneName)
        if bone then
            return bone
        end
    end
    return character:FindFirstChild("HumanoidRootPart")
end

-- Visibility check with wall penetration prevention
local function IsVisible(targetPart, targetCharacter)
    if not targetPart then return false end
    local origin = Camera.CFrame.Position
    local direction = targetPart.Position - origin
    local distance = direction.Magnitude
    direction = direction.Unit

    local ray = Ray.new(origin, direction * distance)
    local ignoreList = {LocalPlayer.Character, Camera}
    local hit = Workspace:FindPartOnRayWithIgnoreList(ray, ignoreList)

    if not hit then return true end
    if hit:IsDescendantOf(targetCharacter) then return true end
    if hit.Transparency > 0.7 then return true end
    return false
end

-- Current locked target
local currentTarget = nil

local function GetAimbotTarget()
    local myChar = LocalPlayer.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return nil end

    local myPos = myChar.HumanoidRootPart.Position
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    local bestTarget = nil
    local bestScore = math.huge

    -- Target lock: keep current if still valid
    if currentTarget and currentTarget.Parent then
        local char = currentTarget.Parent
        local player = Players:GetPlayerFromCharacter(char)
        if player and player ~= LocalPlayer then
            local hum = char:FindFirstChildOfClass("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hum and hum.Health > 0 and hrp then
                local dist = (hrp.Position - myPos).Magnitude
                if dist <= Settings.AimbotRange * 1.3 then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(currentTarget.Position)
                    if onScreen then
                        local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
                        if screenDist <= Settings.AimbotFOV * 1.5 then
                            if IsVisible(currentTarget, char) then
                                return currentTarget
                            end
                        end
                    end
                end
            end
        end
    end
    currentTarget = nil

    -- Find new target
    for _, p in pairs(Players:GetPlayers()) do
        if p == LocalPlayer then continue end
        if not p.Character then continue end

        local char = p.Character
        local bone = GetTargetBone(char)
        local hum = char:FindFirstChildOfClass("Humanoid")
        local hrp = char:FindFirstChild("HumanoidRootPart")

        if not bone or not hum or hum.Health <= 0 or not hrp then continue end
        if Settings.TeamCheck and p.Team == LocalPlayer.Team then continue end

        local dist = (hrp.Position - myPos).Magnitude
        if dist > Settings.AimbotRange then continue end

        local screenPos, onScreen = Camera:WorldToViewportPoint(bone.Position)
        if not onScreen then continue end

        local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
        if screenDist > Settings.AimbotFOV then continue end

        if not IsVisible(bone, char) then continue end

        -- Score: closer to screen center = better, with slight distance weight
        local score = screenDist + (dist * 0.05)
        if score < bestScore then
            bestScore = score
            bestTarget = bone
        end
    end

    currentTarget = bestTarget
    return bestTarget
end

-- ============================================
-- THIRD PERSON
-- ============================================
local tpConnection = nil
local function UpdateThirdPerson()
    if Settings.ThirdPerson then
        if not tpConnection then
            tpConnection = RunService.RenderStepped:Connect(function()
                if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    local hrp = LocalPlayer.Character.HumanoidRootPart
                    local camPos = hrp.Position - hrp.CFrame.LookVector * 7 + Vector3.new(0, 2.5, 0)
                    Camera.CFrame = CFrame.new(camPos, hrp.Position + Vector3.new(0, 1.5, 0))
                end
            end)
        end
    else
        if tpConnection then
            tpConnection:Disconnect()
            tpConnection = nil
        end
    end
end

-- ============================================
-- MAIN LOOP
-- ============================================
RunService.RenderStepped:Connect(function()
    -- ESP
    if Settings.ESP then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                CreateESP(p)
            end
        end
    else
        ClearAllESP()
    end

    -- Aimbot
    if Settings.Aimbot then
        local target = GetAimbotTarget()
        if target then
            local targetCFrame = CFrame.new(Camera.CFrame.Position, target.Position)
            Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, Settings.AimSpeed)
            CircleOutline.Color = Color3.fromRGB(0, 255, 100) -- Green when locked
        else
            CircleOutline.Color = Color3.fromRGB(255, 80, 80) -- Red when idle
        end
    end

    -- Third Person
    UpdateThirdPerson()
end)

-- Cleanup
Players.PlayerRemoving:Connect(function(p)
    ClearPlayerESP(p)
end)

Camera.FieldOfView = Settings.FOV
Notify("FPS Cheat", "Enhanced Script Loaded!", 3)
